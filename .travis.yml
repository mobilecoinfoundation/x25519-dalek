language: rust

rust:
  - nightly

env:
  - TEST_COMMAND=test EXTRA_FLAGS='' FEATURES='default'
  - TEST_COMMAND=bench EXTRA_FLAGS='' FEATURES='default'
  - TEST_COMMAND=build EXTRA_FLAGS='--no-default-features --cfg curve25519_dalek_bits="32"'
  - TEST_COMMAND=build EXTRA_FLAGS='--no-default-features --cfg curve25519_dalek_bits="64"'

matrix:
  include:
    - rust: stable
      env: TEST_COMMAND=test EXTRA_FLAGS='--no-default-features'
    - rust: beta
      env: TEST_COMMAND=test EXTRA_FLAGS='--no-default-features'

script:
  - cargo $TEST_COMMAND --features="$FEATURES" $EXTRA_FLAGS

notifications:
  slack:
    rooms:
      - dalek-cryptography:Xxv9WotKYWdSoKlgKNqXiHoD#dalek-bots
